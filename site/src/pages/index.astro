---
import { getEntry } from 'astro:content';
import ResponsiveFigure from '../components/ResponsiveFigure.astro';
import FailureSequence from '../components/FailureSequence.astro';
import RecoveryMechanism from '../components/RecoveryMechanism.astro';
import { citationBibtex } from '../content/citation';
import BaseLayout from '../layouts/BaseLayout.astro';

const entry = await getEntry('projects', 'on-manifold-tfg');
if (!entry) throw new Error('Project content is missing.');

const project = entry.data;
const base = import.meta.env.BASE_URL;
const pageTitle = `${project.title} | ECCV 2026`;
const pageDescription = 'Prediction target determines whether training-free guidance fails gracefully or catastrophically. This page documents the mechanism, benchmark, controlled evidence, and released resources.';
const titleParts = project.title.split('Training-Free');
---

<BaseLayout title={pageTitle} description={pageDescription} abstract={project.abstract}>
  <header class="site-header">
    <a class="manluml-link" href="https://manluml.github.io/" aria-label="Return to ManLuML home">
      <img src={`${base}brand/manluml-mascot.svg`} width="36" height="36" alt="" />
      <span>ManLuML</span>
    </a>
    <nav aria-label="Project navigation">
      <a href="#abstract">Thesis</a>
      <a href="#benchmark">Benchmark</a>
      <a href="#resources">Resources</a>
    </nav>
  </header>

  <main id="main-content">
    <header class="project-hero" id="top">
      <div class="hero-heading">
        <p class="venue">{project.venue}</p>
        <h1>{titleParts[0]}<span class="nowrap">Training-Free</span>{titleParts[1]}</h1>
      </div>

      <div class="hero-grid">
        <div class="hero-copy">
        <p class="authors">
          {project.authors.map((author, index) => (
            <>
              {index > 0 && <span aria-hidden="true"> · </span>}
              <a href={author.profileUrl}>{author.name}</a><sup>{author.marker}</sup>
            </>
          ))}
        </p>

        <ol class="affiliations" aria-label="Author affiliations">
          {project.authors.map((author, index) => (
            <li><sup>{index + 1}</sup> {author.affiliation}</li>
          ))}
        </ol>

        <p class="contribution-legend"><span>* Equal contribution</span><span>† Corresponding author</span></p>
        <p class="author-contact">
          <a href={`mailto:${project.authors[0].email}`}>{project.authors[0].email}</a>
          <span aria-hidden="true"> · </span>
          <a href={`mailto:${project.authors[1].email}`}>{project.authors[1].email}</a>
        </p>

        <nav class="resource-actions" aria-label="Primary project resources">
          <a class="primary" href={project.links.paper}>Paper <span aria-hidden="true">↗</span></a>
          <a href={project.links.code}>Code <span aria-hidden="true">↗</span></a>
          <a href="#resources">Data <span aria-hidden="true">↓</span></a>
          <a href="#bibtex">Cite <span aria-hidden="true">↓</span></a>
        </nav>
        </div>

        <aside class="hero-thesis" aria-label="Headline result">
          <p class="thesis-label">Gradient-based TFG robustness</p>
          <ol class="robustness-ranking" aria-label="Prediction targets ranked from most to least robust for training-free guidance">
            <li class="x"><span>1</span><strong>x-prediction</strong><em>most robust</em></li>
            <li class="v"><span>2</span><strong>v-prediction</strong></li>
            <li class="epsilon"><span>3</span><strong>ε-prediction</strong><em>least robust</em></li>
          </ol>
          <p>For the gradient-based methods studied here, direct x-prediction avoids recovery amplification during early steps, when global structure is set. v-recovery stays bounded, while ε-recovery can amplify error at high noise.</p>
        </aside>
      </div>
    </header>

    <section class="thesis-section" id="abstract" aria-labelledby="thesis-heading">
      <div class="section-intro">
        <p class="kicker">In one sentence</p>
        <h2 id="thesis-heading">Prediction target changes the failure mode.</h2>
        <p class="thesis-lede">The gradient-based TFG methods studied here act through an estimate of the clean image. With direct x-prediction, guidance can miss the target while the sample remains realistic. For ε-prediction, clean-image recovery multiplies prediction error by <math><mrow><mo>(</mo><mn>1</mn><mo>−</mo><mi>t</mi><mo>)</mo><mo>/</mo><mi>t</mi></mrow></math>, which diverges as <math><mrow><mi>t</mi><mo>→</mo><mn>0</mn></mrow></math> and can send the sample off the manifold.</p>
      </div>

      <figure class="teaser-figure">
        <picture>
          <source type="image/avif" srcset={`${base}media/figures/teaser-720.avif 720w, ${base}media/figures/teaser-1200.avif 1200w, ${base}media/figures/teaser-1800.avif 1800w`} sizes="(max-width: 760px) calc(100vw - 2rem), 1120px" />
          <source type="image/webp" srcset={`${base}media/figures/teaser-720.webp 720w, ${base}media/figures/teaser-1200.webp 1200w, ${base}media/figures/teaser-1800.webp 1800w`} sizes="(max-width: 760px) calc(100vw - 2rem), 1120px" />
          <img
            src={`${base}media/figures/teaser-1200.png`}
            srcset={`${base}media/figures/teaser-720.png 720w, ${base}media/figures/teaser-1200.png 1200w, ${base}media/figures/teaser-1800.png 1800w`}
            sizes="(max-width: 760px) calc(100vw - 2rem), 1120px"
            width="1200"
            height="458"
            loading="eager"
            decoding="async"
            alt="Diagram and examples showing x-prediction moving along the data manifold, epsilon-prediction collapsing off it, and v-prediction failing more gracefully"
          />
        </picture>
        <figcaption>A guided sample may hit the target, miss while remaining realistic, or leave the data manifold entirely. Prediction target changes which failure occurs.</figcaption>
      </figure>
    </section>

    <section class="content-section benchmark-section" id="benchmark" aria-labelledby="benchmark-heading">
      <div class="section-intro">
        <p class="kicker">01 · A practical TFG benchmark</p>
        <h2 id="benchmark-heading">Can guidance hit the target and keep the image realistic?</h2>
        <p class="thesis-lede">Training-free guidance steers a pretrained model at inference time without updating its weights. In our benchmark, the model starts from one of 30 ImageNet bird classes and an external guide requests one of 143 species. The benchmark measures whether guidance reaches the requested species and whether the generated distribution remains realistic.</p>
      </div>

      <ol class="task-flow" aria-label="Realistic training-free guidance evaluation task">
        <li><span>1</span><div><h3>Parent class</h3><p>CFG selects one of 30 ImageNet bird classes already represented in the frozen model.</p></div></li>
        <li><span>2</span><div><h3>Species target</h3><p>The external guide requests one of 143 child species. The model weights do not change.</p></div></li>
        <li><span>3</span><div><h3>Separate evaluation</h3><p>A second classifier measures validity. Child FID and Precision/Recall measure realism and coverage.</p></div></li>
        <li><span>4</span><div><h3>Guidance sweep</h3><p>We evaluate the trade-off across guidance strengths rather than report one selected setting.</p></div></li>
      </ol>

      <dl class="benchmark-facts" aria-label="Benchmark scale and coverage">
        <div><dt>Fine-grained targets</dt><dd>143 species</dd></div>
        <div><dt>Coarse priors</dt><dd>30 parents</dd></div>
        <div><dt>Every operating point</dt><dd>9,152 images</dd></div>
        <div><dt>Model coverage</dt><dd>6 configs · 3 targets · 2 spaces</dd></div>
      </dl>
      <p class="benchmark-definition"><strong>Child FID</strong> compares all 9,152 guided samples, pooled across 143 targets, with the full fine-grained bird reference set. The pooled score rises when classifier-valid outputs drift from realistic bird imagery.</p>
    </section>

    <section class="content-section failure-section" id="results" aria-labelledby="failure-heading">
      <div class="section-intro">
        <p class="kicker">02 · What failure looks like</p>
        <h2 id="failure-heading">A wrong bird can still look like a bird.</h2>
        <p class="thesis-lede">Validity only asks whether the evaluation classifier predicts the requested species. Under stronger guidance, a graceful miss remains coherent; a catastrophic miss becomes an artifact that activates the classifier.</p>
      </div>
      <FailureSequence />
    </section>

    <section class="content-section method-section" id="method" aria-labelledby="method-heading">
      <div class="section-intro">
        <p class="kicker">03 · Why targets differ</p>
        <h2 id="method-heading">Recovery changes the size of prediction error.</h2>
        <p class="thesis-lede">The paper uses the flow-matching convention <math><mrow><msub><mi>z</mi><mi>t</mi></msub><mo>=</mo><mi>t</mi><mi>x</mi><mo>+</mo><mo>(</mo><mn>1</mn><mo>−</mo><mi>t</mi><mo>)</mo><mi>ε</mi></mrow></math>. Each target can produce a clean-image estimate. ε-prediction is the only one whose recovery formula divides by t.</p>
      </div>
      <RecoveryMechanism />
    </section>

    <section class="content-section controlled-section" aria-labelledby="controlled-heading">
      <div class="section-intro">
        <p class="kicker">04 · Controlled attribution</p>
        <h2 id="controlled-heading">Holding the model fixed isolates the target.</h2>
        <p class="thesis-lede">The crossed-lines study uses the same residual MLP architecture, data, training budget, guidance, and sampling for all three prediction objectives. The gap grows with ambient dimension.</p>
      </div>

      <div class="controlled-grid">
        <ResponsiveFigure
          slug="crossed-lines-grid"
          widths={[720, 1400]}
          fallback="png"
          width={1400}
          height={1768}
          alt="Five rows of crossed-lines results from dimension 2 through 512; x-prediction stays concentrated on the target line while v-prediction spreads and epsilon-prediction largely leaves the line at high dimension"
          caption="Crossed-lines under DPS guidance with strength 10 and 100 Euler steps. Each target uses the same 256-hidden, five-block residual MLP design. At D=512, the on-manifold rates are 93.3%, 21.5%, and 0.5% for x-, v-, and ε-prediction."
        />

        <div class="controlled-copy">
          <div class="headline-table table-scroll" tabindex="0" aria-label="D equals 512 on-manifold results, horizontally scrollable">
            <table>
              <caption>On-manifold rate at D = 512</caption>
              <thead><tr><th scope="col">Prediction target</th><th scope="col">Rate</th><th scope="col">Failure character</th></tr></thead>
              <tbody>
                <tr><th scope="row">x</th><td class="best">93.3%</td><td>Structure preserved</td></tr>
                <tr><th scope="row">v</th><td>21.5%</td><td>Bounded but degraded</td></tr>
                <tr><th scope="row">ε</th><td class="worst">0.5%</td><td>Near-total collapse</td></tr>
              </tbody>
            </table>
          </div>

          <div class="control-lines" aria-label="Converging controls">
            <article>
              <p class="control-number">1</p>
              <div><h3>Matched networks</h3><p>Three residual MLPs share the same architecture, data, and training setup across D ∈ &#123;2, 8, 32, 128, 512&#125;. Only the prediction target changes.</p></div>
            </article>
            <article>
              <p class="control-number">2</p>
              <div><h3>Latent-space control</h3><p>DiT and SiT share their architecture family, scale, training data, and operating space. Their ordering is consistent with the target effect.</p></div>
            </article>
            <article>
              <p class="control-number">3</p>
              <div><h3>Smaller x-prediction model</h3><p>JiT-B has 131M diffusion parameters yet reaches C-FID 31.3, ahead of 675M-parameter DiT at 36.7 and SiT at 34.4.</p></div>
            </article>
            <article>
              <p class="control-number">4</p>
              <div><h3>Pixel-space control</h3><p>JiT and PixelFlow both avoid a latent VAE, which provides a complementary comparison within pixel space.</p></div>
            </article>
          </div>
        </div>
      </div>
    </section>

    <section class="content-section real-image-section" aria-labelledby="real-heading">
      <div class="section-intro">
        <p class="kicker">05 · Real-image evidence</p>
        <h2 id="real-heading">Classifier validity can hide manifold damage.</h2>
        <p class="thesis-lede"><strong>Child FID</strong> compares the pooled guided distribution across all 143 targets with the full fine-grained bird reference set. The score worsens when classifier-valid outputs drift from realistic bird imagery.</p>
      </div>

      <div class="metric-callouts" aria-label="Matched validity Child FID results">
        <article class="x"><p>x · JiT-H</p><strong>32.9</strong><span>Child FID</span></article>
        <article class="v"><p>v · SiT</p><strong>34.7</strong><span>Child FID</span></article>
        <article class="epsilon"><p>ε · DiT</p><strong>38.1</strong><span>Child FID</span></article>
        <article class="gap"><p>x → ε gap</p><strong>5.2</strong><span>points at matched validity</span></article>
      </div>

      <p class="sample-scale"><strong>Benchmark scale:</strong> 143 bird species are nested under 30 ImageNet parents. Every plotted point contains 9,152 generated images, with 64 samples per species. The matched operating points are all near 26.6% validity.</p>

      <div class="plot-grid">
        <ResponsiveFigure
          slug="rho-fid-vs-validity"
          widths={[720, 1200]}
          fallback="png"
          width={1200}
          height={867}
          alt="Plot of parent FID against classifier validity; several models reach similar validity while their parent FID trade-offs differ"
          caption="Validity records whether the evaluation classifier predicts the requested species. Similar validity does not imply similar visual fidelity."
        />
        <ResponsiveFigure
          slug="rho-fid-vs-child-fid"
          widths={[720, 1200]}
          fallback="png"
          width={1200}
          height={867}
          alt="Plot of parent FID against Child FID; JiT x-prediction traces the lowest Child FID frontier, SiT v-prediction is intermediate, and DiT epsilon-prediction remains higher"
          caption="Child FID reveals the quality gap at matched validity: 32.85 for JiT-H, 34.66 for SiT, and 38.11 for DiT. Lower is better."
        />
      </div>

      <div class="matched-table table-scroll" tabindex="0" aria-label="Matched validity results, horizontally scrollable">
        <table>
          <caption>Matched-validity operating points reported in the paper</caption>
          <thead><tr><th scope="col">Model</th><th scope="col">Target</th><th scope="col">Guidance ρ</th><th scope="col">Validity</th><th scope="col">P-FID</th><th scope="col">C-FID</th></tr></thead>
          <tbody>
            <tr><th scope="row">JiT-H/16</th><td>x</td><td>3.0</td><td>26.60%</td><td>6.91</td><td class="best">32.85</td></tr>
            <tr><th scope="row">SiT-XL/2</th><td>v</td><td>1.0</td><td>26.64%</td><td>8.21</td><td>34.66</td></tr>
            <tr><th scope="row">DiT-XL/2</th><td>ε</td><td>0.1</td><td>26.69%</td><td>6.70</td><td class="worst">38.11</td></tr>
          </tbody>
        </table>
      </div>
    </section>

    <section class="content-section broader-section" aria-labelledby="broader-heading">
      <div class="section-intro">
        <p class="kicker">06 · Broader validation</p>
        <h2 id="broader-heading">The pattern repeats, with a smaller gap in style transfer.</h2>
        <p class="thesis-lede">We test two more guidance rules, a second fine-grained domain, style transfer, Precision/Recall, and inverse problems.</p>
      </div>

      <div class="validation-grid">
        <article class="validation-card">
          <ResponsiveFigure slug="lgd" widths={[640, 960]} fallback="png" width={960} height={693} alt="LGD bird benchmark frontier with JiT-H x-prediction attaining the lowest Child FID" caption="LGD on the fine-grained bird benchmark." />
          <h3>LGD</h3><p class="answer">JiT-H retains the lowest C-FID frontier.</p><a href={project.links.paper}>Paper details ↗</a>
        </article>
        <article class="validation-card">
          <ResponsiveFigure slug="freedom" widths={[640, 960]} fallback="png" width={960} height={694} alt="FreeDoM bird benchmark frontier with JiT-H x-prediction attaining the lowest Child FID" caption="FreeDoM on the fine-grained bird benchmark." />
          <h3>FreeDoM</h3><p class="answer">JiT-H again has the lowest C-FID frontier.</p><a href={project.links.paper}>Paper details ↗</a>
        </article>
        <article class="validation-card">
          <ResponsiveFigure slug="butterfly" widths={[640, 960]} fallback="png" width={960} height={696} alt="Butterfly benchmark P-FID versus Child FID plot, with JiT-H x-prediction on the lowest frontier" caption="Thirty-four butterfly species under six ImageNet parents." />
          <h3>Butterfly benchmark</h3><p class="answer">The x, v, ε ordering also appears across 34 butterfly species.</p><a href={project.links.datasets[1].url}>Butterfly data ↗</a>
        </article>
        <article class="validation-card">
          <ResponsiveFigure slug="style" widths={[640, 960]} fallback="png" width={960} height={693} alt="Style-transfer plot of Gram loss against content validity; x-prediction retains content accuracy more strongly at high guidance" caption="Four WikiArt styles by 100 ImageNet classes, 400 images per setting." />
          <h3>Style transfer</h3><p class="answer">The separation is smaller than on birds. At ρ=10, JiT-H retains 80% content accuracy while DiT falls to 1.5%.</p><a href={project.links.paper}>Paper details ↗</a>
        </article>
        <article class="validation-card">
          <ResponsiveFigure slug="precision-recall" widths={[640, 960]} fallback="png" width={960} height={675} alt="Precision versus recall plot; JiT-H reaches recall 0.59 while DiT remains near 0.49 and loses precision under stronger guidance" caption="PRDC separates fidelity from coverage and exposes mode-collapse behavior." />
          <h3>Precision and recall</h3><p class="answer">JiT-H reaches 0.59 recall. DiT peaks at 0.24 precision, stays near 0.49 recall, and loses precision as guidance strengthens.</p><a href={project.links.paper}>Paper details ↗</a>
        </article>
        <article class="validation-card inverse-card">
          <div class="inverse-table table-scroll" tabindex="0" aria-label="Inverse problem LPIPS results, horizontally scrollable">
            <table>
              <caption>Best x-prediction perceptual results</caption>
              <thead><tr><th scope="col">Task</th><th scope="col">Best LPIPS</th><th scope="col">ρ</th></tr></thead>
              <tbody><tr><th scope="row">Gaussian deblur</th><td>0.2140</td><td>16</td></tr><tr><th scope="row">4× super-resolution</th><td>0.1886</td><td>24</td></tr></tbody>
            </table>
          </div>
          <h3>Inverse problems</h3><p class="answer">x-prediction reaches the best LPIPS among the evaluated models on both tasks and remains stable over a wider range of ρ. No model beats the degraded-input PSNR baseline, so the result is perceptual rather than a universal reconstruction win.</p><a href={project.links.code}>Reproduction code ↗</a>
        </article>
      </div>
    </section>

    <section class="content-section resources-section" id="resources" aria-labelledby="resources-heading">
      <div class="section-intro">
        <p class="kicker">Resources and citation</p>
        <h2 id="resources-heading">Paper, code, data, and citation.</h2>
      </div>

      <div class="resources-grid">
        <article><p class="resource-type">Paper</p><h3>ECCV 2026 · accepted</h3><p>ECCV's official listing confirms the title and authors. The manuscript is available on arXiv.</p><a href={project.links.official}>View the ECCV record ↗</a><a href={project.links.paper}>Open the PDF ↗</a><a href={project.links.paperAbstract}>View the arXiv record ↗</a></article>
        <article><p class="resource-type">Code</p><h3>Evaluation and inference</h3><p>The repository contains the crossed-lines study, bird benchmark, and inverse-problem evaluations.</p><a href={project.links.code}>Open the repository ↗</a></article>
        <article class="datasets"><p class="resource-type">Data</p><h3>Published benchmark resources</h3><ul>{project.links.datasets.map((dataset) => <li><a href={dataset.url}>{dataset.label} ↗</a></li>)}</ul></article>
      </div>

      <div class="citation-card" id="bibtex">
        <div>
          <p class="resource-type">Cite</p>
          <h3>Provisional ECCV BibTeX</h3>
        </div>
        <button type="button" data-copy-bibtex aria-describedby="copy-status">Copy BibTeX</button>
        <pre id="bibtex-text" tabindex="0" aria-label="BibTeX citation, horizontally scrollable"><code>{citationBibtex}</code></pre>
        <p class="citation-source">ECCV confirms the title, authors, and acceptance. The paper does not yet have a public Springer volume, page range, or DOI, so the citation omits those fields and uses "To appear."</p>
        <p id="copy-status" class="copy-status" role="status" aria-live="polite">If clipboard access is unavailable, select and copy the citation directly.</p>
      </div>

      <aside class="rights-note" aria-label="Rights and attribution">
        <h3>Rights and licenses</h3>
        <p>The repository license covers the website code. Paper text and figures remain © 2026 Yunsung Lee and Hyeongmin Lee. The ManLuML mascot is a separate brand asset. This project page presents affiliations as text and includes no institution logo files. Self-hosted Inter is distributed under the <a href={`${base}licenses/inter-OFL.txt`}>SIL Open Font License 1.1</a>.</p>
      </aside>
    </section>
  </main>

  <footer class="project-footer">
    <a class="footer-brand" href="https://manluml.github.io/"><img src={`${base}brand/manluml-mascot.svg`} width="42" height="42" alt="" /><span>Return to ManLuML</span></a>
    <p>ECCV 2026 · Yunsung Lee and Hyeongmin Lee</p>
  </footer>

  <script>
    const copyButton = document.querySelector<HTMLButtonElement>('[data-copy-bibtex]');
    const citation = document.querySelector<HTMLElement>('#bibtex-text');
    const status = document.querySelector<HTMLElement>('#copy-status');

    copyButton?.addEventListener('click', async () => {
      if (!citation || !status) return;
      const text = citation.textContent?.trim() ?? '';
      try {
        if (!navigator.clipboard) throw new Error('Clipboard unavailable');
        await navigator.clipboard.writeText(text);
        copyButton.textContent = 'Copied';
        status.textContent = 'BibTeX copied to the clipboard.';
      } catch {
        const selection = window.getSelection();
        const range = document.createRange();
        range.selectNodeContents(citation);
        selection?.removeAllRanges();
        selection?.addRange(range);
        copyButton.textContent = 'Citation selected';
        status.textContent = 'The BibTeX is selected. Copy it with your usual keyboard shortcut.';
      }
      window.setTimeout(() => { if (copyButton) copyButton.textContent = 'Copy BibTeX'; }, 2400);
    });
  </script>
</BaseLayout>

<style>
  .site-header {
    position: sticky;
    z-index: 20;
    top: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    min-height: 4.25rem;
    padding: 0.7rem max(1rem, calc((100vw - 1160px) / 2));
    border-bottom: 1px solid rgb(203 213 225 / 0.85);
    background: rgb(255 255 255 / 0.94);
    backdrop-filter: blur(14px);
  }
  .manluml-link { display: inline-flex; align-items: center; gap: 0.55rem; color: var(--ink); font-weight: 850; text-decoration: none; }
  .manluml-link img { width: 2.2rem; height: 2.2rem; }
  .site-header nav { display: flex; gap: 1.1rem; }
  .site-header nav a { color: var(--slate); font-size: 0.88rem; font-weight: 700; }
  .project-hero {
    width: min(1280px, calc(100% - 2rem));
    margin: 0 auto;
    padding: clamp(3rem, 5vw, 4.5rem) 0 clamp(3.5rem, 6vw, 5rem);
  }
  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.12fr) minmax(20rem, 0.88fr);
    gap: clamp(2.5rem, 7vw, 6rem);
    align-items: start;
    margin-top: 2.5rem;
  }
  .venue, .kicker {
    margin: 0 0 1rem;
    color: var(--navy);
    font-size: 0.78rem;
    font-weight: 850;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }
  h1 {
    max-width: none;
    margin: 0;
    font-size: clamp(2.3rem, 3.55vw, 3.2rem);
    font-weight: 660;
    letter-spacing: -0.044em;
    line-height: 1.03;
    overflow-wrap: normal;
    text-wrap: balance;
  }
  .nowrap { white-space: nowrap; }
  .authors { margin: 0 0 0.5rem; font-size: clamp(1.1rem, 2vw, 1.35rem); overflow-wrap: anywhere; }
  .authors a { color: var(--ink); font-weight: 780; text-decoration-thickness: 0.08em; }
  sup { font-size: 0.65em; font-weight: 800; }
  .affiliations { display: flex; flex-wrap: wrap; gap: 0.25rem 1.5rem; margin: 0; padding: 0; color: var(--slate); overflow-wrap: anywhere; list-style: none; }
  .contribution-legend { display: flex; flex-wrap: wrap; gap: 0.4rem 1.25rem; margin: 1.1rem 0 0; padding-top: 0.85rem; border-top: 1px solid var(--line); color: var(--muted); font-size: 0.82rem; }
  .author-contact { margin: 0.35rem 0 0; color: var(--muted); font-size: 0.82rem; overflow-wrap: anywhere; }
  .author-contact a { color: var(--slate); }
  .resource-actions { display: flex; flex-wrap: wrap; gap: 0.65rem; margin-top: 2rem; }
  .resource-actions a {
    display: inline-flex;
    min-height: 2.9rem;
    align-items: center;
    gap: 0.35rem;
    padding: 0.55rem 0.2rem;
    border: 0;
    border-bottom: 1px solid #8da4c6;
    border-radius: 0;
    background: transparent;
    color: var(--navy-dark);
    font-weight: 780;
    text-decoration: none;
  }
  .resource-actions .primary { padding-inline: 0.9rem; border-bottom-color: var(--navy); border-radius: 0.35rem; background: var(--navy); color: white; }
  .hero-thesis {
    padding: 1.15rem 0 0;
    border: 0;
    border-top: 2px solid var(--navy);
    border-radius: 0;
    background: transparent;
  }
  .thesis-label { margin: 0; color: var(--muted); font-size: 0.72rem; font-weight: 850; letter-spacing: 0.15em; text-transform: uppercase; }
  .robustness-ranking { margin: 0.6rem 0 1.1rem; padding: 0; list-style: none; }
  .robustness-ranking li { display: grid; grid-template-columns: 1.45rem minmax(0, 1fr) auto; align-items: baseline; gap: 0.55rem; padding: 0.45rem 0; border-top: 1px solid var(--line); }
  .robustness-ranking li > span { display: grid; width: 1.35rem; height: 1.35rem; place-items: center; border-radius: 50%; background: var(--navy); color: white; font-size: 0.7rem; font-style: normal; font-weight: 800; }
  .robustness-ranking li.v > span { background: var(--cyan-dark); }
  .robustness-ranking li.epsilon > span { background: var(--error); }
  .robustness-ranking strong { font-size: 0.92rem; }
  .robustness-ranking em { color: var(--muted); font-size: 0.68rem; font-style: normal; font-weight: 650; text-transform: uppercase; }
  .hero-thesis > p:not(.thesis-label) { margin-bottom: 0; color: var(--slate); }
  .thesis-section { padding: 7rem max(1rem, calc((100vw - 1160px) / 2)); border-top: 1px solid var(--line); background: var(--paper); }
  .section-intro { max-width: 56rem; }
  h2 { max-width: 17ch; margin: 0; font-size: clamp(2.2rem, 4.2vw, 3.75rem); font-weight: 630; letter-spacing: -0.043em; line-height: 1.06; overflow-wrap: anywhere; }
  .thesis-lede { max-width: 48rem; margin: 1.5rem 0 0; color: var(--slate); font-size: clamp(1.12rem, 2vw, 1.32rem); }
  math { font-family: Cambria Math, STIX Two Math, Times New Roman, serif; }
  .teaser-figure { margin: 3rem 0 0; }
  .teaser-figure picture { display: block; overflow: hidden; border: 1px solid var(--line); border-radius: 0; background: white; }
  .teaser-figure img { width: 100%; }
  .teaser-figure figcaption { max-width: 60rem; margin: 1rem 0 0; color: var(--slate); font-size: 0.9rem; }
  .content-section {
    padding: 7rem max(1rem, calc((100vw - 1160px) / 2));
    border-top: 1px solid var(--line);
  }
  .content-section { background: white; }
  .method-section { background: var(--paper); }
  #abstract, #benchmark, #results, #method, #resources, #bibtex { scroll-margin-top: 5.5rem; }
  .task-flow { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); margin: 3rem 0 0; padding: 0; border-block: 1px solid var(--line); list-style: none; }
  .task-flow li { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 0.8rem; align-content: start; padding: 1.25rem 1.15rem 1.35rem 0; }
  .task-flow li + li { padding-left: 1.15rem; border-left: 1px solid var(--line); }
  .task-flow span { display: grid; width: 1.65rem; height: 1.65rem; place-items: center; border-radius: 50%; background: var(--navy); color: white; font-size: 0.72rem; font-weight: 850; }
  .task-flow h3 { margin: 0; font-size: 1rem; }
  .task-flow p { margin: 0.3rem 0 0; color: var(--slate); font-size: 0.86rem; }
  .benchmark-facts { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); margin: 2.25rem 0 0; }
  .benchmark-facts div { padding: 0.9rem 1rem 0 0; border-top: 2px solid var(--navy); }
  .benchmark-facts div + div { padding-left: 1rem; }
  .benchmark-facts dt { color: var(--muted); font-size: 0.74rem; font-weight: 750; letter-spacing: 0.05em; text-transform: uppercase; }
  .benchmark-facts dd { min-width: 0; margin: 0.35rem 0 0; overflow-wrap: anywhere; font-size: clamp(1.15rem, 2.3vw, 1.65rem); font-weight: 760; letter-spacing: -0.025em; line-height: 1.15; }
  .benchmark-definition {
    max-width: 62rem;
    margin: 2rem 0 0;
    padding: 1rem 0 0;
    border-top: 1px solid var(--line);
    color: var(--slate);
  }
  .sample-scale {
    max-width: 62rem;
    margin: 1.5rem 0 0;
    padding: 1rem 1.15rem;
    border-left: 0.25rem solid var(--navy);
    background: #edf4ff;
    color: var(--slate);
  }
  .controlled-grid {
    display: grid;
    grid-template-columns: minmax(18rem, 0.78fr) minmax(0, 1.22fr);
    gap: clamp(1.5rem, 4vw, 3rem);
    align-items: start;
    margin-top: 3rem;
  }
  .controlled-copy { position: sticky; top: 6.3rem; }
  .table-scroll { overflow-x: auto; border-block: 1px solid var(--line); border-radius: 0; background: transparent; }
  table { width: 100%; border-collapse: collapse; }
  caption { padding: 0.8rem; color: var(--slate); font-size: 0.8rem; font-weight: 750; text-align: left; }
  th, td { padding: 0.72rem 0.8rem; border-top: 1px solid var(--line); text-align: left; font-variant-numeric: tabular-nums; }
  thead th { color: var(--slate); font-size: 0.76rem; letter-spacing: 0.04em; text-transform: uppercase; }
  td { color: var(--slate); }
  .best { color: var(--success); font-weight: 850; }
  .worst { color: var(--error); font-weight: 850; }
  .control-lines { margin-top: 1rem; }
  .control-lines article { display: grid; grid-template-columns: auto minmax(0, 1fr); gap: 0.85rem; padding: 1rem 0; border-top: 1px solid var(--line); }
  .control-number { display: grid; width: 2rem; height: 2rem; place-items: center; margin: 0; border-radius: 50%; background: var(--navy); color: white; font-weight: 850; }
  .control-lines h3 { margin: 0; font-size: 1rem; }
  .control-lines article div p { margin: 0.25rem 0 0; color: var(--slate); font-size: 0.9rem; }
  .metric-callouts { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1.25rem; margin: 3rem 0 1rem; padding-block: 1.3rem; border-block: 1px solid var(--line); }
  .metric-callouts article { padding: 0; border: 0; border-top: 2px solid var(--navy); border-radius: 0; background: transparent; }
  .metric-callouts article.v { border-top-color: var(--cyan-dark); }
  .metric-callouts article.epsilon { border-top-color: var(--error); background: transparent; }
  .metric-callouts article.gap { border-top-color: var(--slate); }
  .metric-callouts p { margin: 0; color: var(--muted); font-size: 0.75rem; font-weight: 780; text-transform: uppercase; }
  .metric-callouts strong { display: block; margin: 0.25rem 0; font-size: clamp(1.9rem, 3.5vw, 3rem); font-weight: 650; line-height: 1; }
  .metric-callouts span { color: var(--slate); font-size: 0.78rem; }
  .plot-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1.2rem; margin-top: 2.5rem; }
  .matched-table { margin-top: 2rem; }
  .matched-table table { min-width: 46rem; }
  .validation-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 1rem; margin-top: 3rem; }
  .validation-card { min-width: 0; padding: 1.5rem 0 0; border: 0; border-top: 1px solid var(--line); border-radius: 0; background: transparent; }
  .validation-card h3 { margin: 1rem 0 0.3rem; font-size: 1.25rem; }
  .validation-card .answer { min-height: 4.7rem; margin: 0 0 0.7rem; color: var(--slate); }
  .validation-card > a { font-weight: 750; }
  .inverse-card { display: flex; flex-direction: column; justify-content: flex-end; background: transparent; }
  .inverse-table { width: 100%; max-width: 100%; }
  .inverse-table table { min-width: 28rem; }
  .resources-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 0.8rem; margin-top: 3rem; }
  .resources-grid article { padding: 1rem 0 0; border: 0; border-top: 1px solid var(--line); border-radius: 0; background: transparent; }
  .resource-type { margin: 0; color: var(--navy); font-size: 0.72rem; font-weight: 850; letter-spacing: 0.13em; text-transform: uppercase; }
  .resources-grid h3, .citation-card h3, .rights-note h3 { margin: 0.35rem 0; font-size: 1.35rem; }
  .resources-grid article > p:not(.resource-type) { color: var(--slate); }
  .resources-grid article > a { display: block; margin-top: 0.45rem; font-weight: 750; }
  .datasets ul { margin: 0.75rem 0 0; padding-left: 1.2rem; }
  .datasets li + li { margin-top: 0.45rem; }
  .citation-card {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 1rem;
    align-items: start;
    margin-top: 1rem;
    padding: 1.2rem;
    border: 1px solid var(--line);
    border-radius: var(--radius);
    background: var(--ink);
    color: white;
  }
  .citation-card .resource-type { color: #83d5e6; }
  .citation-card button { min-height: 2.7rem; padding: 0.55rem 0.9rem; border: 1px solid #83d5e6; border-radius: 999px; background: transparent; color: white; font-weight: 780; cursor: pointer; }
  .citation-card pre { grid-column: 1 / -1; overflow-x: auto; margin: 0; padding: 1rem; border-radius: 0.55rem; background: #111827; color: #edf4ff; font-size: 0.86rem; line-height: 1.55; white-space: pre; }
  .citation-source, .copy-status { grid-column: 1 / -1; margin: 0; color: #cad7ea; font-size: 0.8rem; }
  .citation-source { max-width: 68rem; }
  .rights-note { margin-top: 1.5rem; padding: 1rem 0 0; border: 0; border-top: 1px solid var(--line); border-radius: 0; background: transparent; }
  .rights-note p { margin-bottom: 0; color: var(--slate); font-size: 0.88rem; }
  .project-footer { display: flex; align-items: center; justify-content: space-between; gap: 1rem; width: var(--content); margin: 0 auto; padding: 2rem 0; color: var(--muted); font-size: 0.85rem; }
  .footer-brand { display: inline-flex; align-items: center; gap: 0.55rem; color: var(--ink); font-weight: 800; text-decoration: none; }
  .footer-brand img { width: 2.5rem; height: 2.5rem; }
  .project-footer p { margin: 0; }

  @media (max-width: 960px) {
    .task-flow, .benchmark-facts { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    .task-flow li:nth-child(3) { border-left: 0; }
    .task-flow li:nth-child(n + 3) { border-top: 1px solid var(--line); }
  }

  @media (max-width: 860px) {
    .hero-grid { grid-template-columns: minmax(0, 1fr); }
    .controlled-grid { grid-template-columns: minmax(0, 1fr); }
    .controlled-copy { position: static; }
    .metric-callouts { grid-template-columns: repeat(2, 1fr); }
    .plot-grid { grid-template-columns: 1fr; }
    .resources-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 560px) {
    .site-header { align-items: flex-start; flex-wrap: wrap; }
    .site-header nav { margin-left: auto; }
    .site-header nav a:not(:last-child) { display: none; }
    .project-hero { padding-top: 3.5rem; }
    h1 { font-size: 2rem; line-height: 1.06; }
    .resource-actions a { flex: 1 1 calc(50% - 0.65rem); justify-content: center; }
    .thesis-section { padding-block: 5rem; }
    .content-section { padding-block: 5rem; }
    .task-flow, .benchmark-facts, .metric-callouts, .validation-grid { grid-template-columns: 1fr; }
    .task-flow li, .task-flow li + li { padding: 1rem 0; border-left: 0; border-top: 1px solid var(--line); }
    .task-flow li:first-child { border-top: 0; }
    .benchmark-facts div, .benchmark-facts div + div { padding: 0.9rem 0 0; }
    .validation-card .answer { min-height: auto; }
    .citation-card { grid-template-columns: 1fr; }
    .citation-card button { justify-self: start; }
    .citation-card pre, .citation-source, .copy-status { grid-column: 1; }
    .project-footer { align-items: flex-start; flex-direction: column; }
  }
  @media (max-width: 360px) {
    .nowrap { white-space: normal; }
  }
</style>
