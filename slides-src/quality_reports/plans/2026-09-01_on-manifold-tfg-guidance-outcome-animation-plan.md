# On-Manifold TFG Guidance-Outcome Animation Plan

## Goal

Turn physical slide 12 into a staged explanation of success, catastrophic
failure, graceful failure, and the paper's meaning of on-manifold generation.

## Reference

- Google Slides presentation `1aetX1aL4r5n-uf4-lYu6RbCUJdibNUxaV0pSUsovcrI`
- Retrieved with `gws-personal` at revision `A3DrOVsNeQSPhQ`
- Reference pages 4–7: combined Figure 1(b), then success, catastrophic
  failure, and graceful failure as isolated visual stages

## Implementation

1. Derive the x-, epsilon-, and v-prediction toy plots and the three outcome
   photos from the latest rendered reference pages.
2. Keep the three prediction plots visible as the common baseline.
3. Reveal the corresponding outcome card and one accumulating caption per
   step: success, catastrophic failure, then graceful failure.
4. Add a final fourth reveal defining on-manifold as realism under the parent
   data class, independently of exact target correctness.
5. Shorten the title to `Do prediction targets decide how guidance fails?`
   and keep it on one line.

## Acceptance Criteria

- Four Reveal steps appear on slide 12 without changing its page number.
- Each step reveals one outcome card and appends exactly one bottom caption.
- Success shows target and quality success.
- Catastrophic failure shows target, parent-class, and quality failure and is
  explicitly off-manifold.
- Graceful failure shows target failure but parent-class and quality success
  and is explicitly on-manifold.
- All final-state content remains inside the 1280 x 720 canvas with no
  page-number collision.
- Public speaker notes remain absent and all local assets resolve.
